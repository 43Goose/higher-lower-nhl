import { Inter } from 'next/font/google';
import { Fugaz_One } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const fugaz = Fugaz_One({ weight: '400' , subsets: ['latin'] });